=DERECHA(A2,LARGO(A2)
-ENCONTRAR("*",
SUSTITUIR(
        A2," ","*",
        LARGO(A2)-LARGO(SUSTITUIR(A2," ",""))
        )
    )
)

=RIGHT(A2,LEN(A2)
-FIND("*",
SUBSTITUTE(
        A2," ","*",
        LEN(A2)-LEN(SUBSTITUTE(A2," ",""))
        )
    )
)
